import { FC } from "react";
// import { useAuthRedirection } from "../../features/auth/hooks/hooks";

const Signup: FC = () => {
  // const {redirectTo, register, user} = useAuthRedirection()

  return <div>

  </div>
}

export default Signup